apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: mongo-pvc
  namespace: streamingapp
spec:
  storageClassName: mongo-local-storage
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 5Gi