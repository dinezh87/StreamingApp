apiVersion: v1
kind: Service
metadata:
  name: admin-service
  namespace: streamingapp
spec:
  selector:
    app: admin
  ports:
    - protocol: TCP
      port: 3003
      targetPort: 3003
  type: ClusterIP