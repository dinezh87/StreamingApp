apiVersion: v1
kind: Service
metadata:
  name: streaming-service
  namespace: streamingapp
spec:
  selector:
    app: streaming
  ports:
  - protocol: TCP
    port: 3002
    targetPort: 3002
  type: ClusterIP